"""Modelos Pydantic, separados por papel.

`domain` são as linhas do Postgres e os resultados do livro-razão; `tools`
é o que o LLM enxerga e devolve nas ferramentas; `reply` é a resposta final
estruturada e o documento extraído por visão. Convenção de nomes no README.
"""
